<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.urv.deim.asm.dev_comm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrimeraApp">
        <activity android:name="cat.urv.deim.asm.dev_comm.ConditionActivity"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name="cat.urv.deim.asm.dev_comm.TutorialActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cat.urv.deim.asm.dev_comm.LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="cat.urv.deim.asm.dev_comm.SplashActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>